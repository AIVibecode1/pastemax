# Implementation Plans

Generated by the improve skill (deep audit) on 2026-08-07. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Repo state at planning time (read this first)

- **No git repository exists yet** (`git rev-parse` fails). Plan 001 creates the baseline commit; every later plan's drift check compares against that commit. **Baseline SHA: `e3d9dd6`** (created by plan 001, 2026-08-07).
- **`node_modules` is absent** — run `npm install` before any verification command.
- No test framework exists; plan 002 adds vitest + `npm test`. Plans that depend on 002 use `npm test` for verification; plans before it use `npx tsc --noEmit` and `npm run lint`.
- All plans are written for the repo at commit baseline (the one created by 001). If the code excerpts in a plan don't match the live files, STOP and report (drift).
- App version string is `v1.1.1-stable`; version bumps are NOT part of any plan unless stated.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Init git repo + hygiene (.gitignore cruft) | P1 | S | — | DONE |
| 002 | Test infrastructure (vitest) + characterization tests | P1 | M | — | DONE |
| 003 | CI quality gates (typecheck + lint:strict) | P1 | S | 001 | DONE |
| 004 | Put user instructions on top of copied content (+ delete drifted twin) | P1 | S | 002 | DONE |
| 005 | Cancel directory scan when window closes | P1 | S | — | DONE |
| 006 | Segment-aware macOS `.app` bundle skip | P1 | S | 002 | DONE |
| 007 | Safe localStorage parse/persist (no white-screen on corrupt data) | P1 | S | — | DONE |
| 008 | Restore selected task type on launch | P1 | S | — | DONE |
| 009 | Fix `isWSLPath` (dead on Windows) | P2 | S | 002 | DONE |
| 010 | HTTP timeouts + don't cache update-check errors | P2 | S | — | DONE |
| 011 | Strip error stacks / raw API bodies from renderer-bound payloads | P2 | S | — | DONE |
| 012 | Propagate top-level scan errors instead of "Found 0 files" | P2 | S | — | DONE |
| 013 | Fix IPC listener cleanup in preload | P2 | S | — | DONE |
| 014 | Copy history quota handling (no false "copy failed") | P2 | S-M | — | DONE |
| 015 | Unify IPC channel whitelist; migrate renderer; delete compat shim | P1 | S-M | 013 | DONE |
| 016 | Navigation handlers: deny everything except http(s)→external | P1 | S | — | DONE |
| 017 | Harden production CSP (drop script-src 'unsafe-inline') | P2 | S-M | — | DONE |
| 018 | Folder-consent boundary for renderer-chosen scan paths | P2 | M | — | DONE |
| 019 | Bump Electron to a supported major | P1 | M | — | DONE |
| 020 | Bump electron-builder (fixes AppImage GHSA-7g7r-gx96-252g) | P1 | S | 019 | DONE |
| 021 | Remediate remaining npm audit tail (no --force) | P2 | S-M | 020 | DONE |
| 022 | Prune dead deps (gpt-3-encoder, lodash, node-fetch) | P2 | S | — | DONE |
| 023 | Keep file cache across scans (key by mtime+size) | P2 | M | 002 | DONE |
| 024 | Sidebar: stop full tree rebuild on expand/collapse | P2 | M | 002 | TODO |
| 025 | useModels refetch fix + move formatting off main thread | P2 | M | 002 | TODO |
| 026 | Precompile GlobalModeExclusion filter | P2 | S | 002 | TODO |
| 027 | Throttle per-directory status IPC | P2 | S | — | TODO |
| 028 | Delete dead files (renderer.js, OldMain.js, backups, dead workflows) | P2 | S | 001 | TODO |
| 029 | Decompose App.tsx (hooks extraction + dead code) | P2 | L | 002, 004 | TODO |
| 030 | Single useIgnorePatterns instance; remove reload hack | P2 | S-M | 002 | TODO |
| 031 | UI redesign: premium visual overhaul (redesign-existing-projects) | P3 | L | 002 (rec.) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always**: no git = no drift checks for any other plan. Commit the current tree as baseline (minus cruft listed in 001).
- **002 before anything that changes behavior or refactors**: 004, 006, 009, 023, 024, 025, 026, 029, 030. The characterization suite pins current behavior so executors can prove their change is the only one.
- **004 before 029**: 004 settles which formatter survives and moves instructions first; 029 then extracts the copy pipeline without ambiguity.
- **013 before 015**: both rewrite `electron/preload.js`; 013 fixes the listener map, 015 then unifies whitelists and deletes the compat shim. Do not run in parallel.
- **019 → 020 → 021**: one dependency pipeline, same lockfile and same rebuild verification (`npm run test-build`). Run sequentially, never in parallel.
- **028 after 001**: deleting files only makes sense once git history exists (001 baseline) so nothing is lost.
- **005, 012, 010, 016, 018, 019 touch the main process and its startup behavior** — smoke-test the packaged app (or `npm start`) after each.
- **031 is a visual-only redesign** (direction plan, created on request with the `redesign-existing-projects` skill): run it AFTER the P1 functional/security batch (004-018) and AFTER 024/029/030 (same components — never in parallel). Its font step (self-hosted @fontsource) must land BEFORE 017's CSP tightening, which then drops the Google Fonts sources. Recommended to run with 002's suite as the regression net.

## Findings considered and rejected

- React 19 / Vite 7 major upgrades: not worth doing — high effort/risk for a single-window desktop app with no benefit to the shipped product.
- macOS signing/notarization: documented by-design (README "Not trusted" section); `hardenedRuntime: false` is consistent with that decision.
- `exec('wsl.exe --list --quiet')`: static command, no injection surface — audited, no action.
- Lockfile churn from `npm update --save`: not planned; dependency work is handled by 019-022 only.
- `lodash` advisory (GHSA-r5fr-rjxr-66jc): no fixed version exists; removing the single `debounce` use is covered by 022, which also removes the audit noise.
